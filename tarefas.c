#include "tarefas.h"
#include <stdio.h>

Erro criar(Tarefa t[], int *pos) {
  printf("função criar tarefa");
  return OK;
}
Erro deletar(Tarefa t[], int *pos) {
  printf("função deletar tarefa");
  return OK;
}
Erro listar(Tarefa t[], int pos) {
  printf("função listar tarefa");
  return OK;
}
Erro salvar(Tarefa t[], int pos, int tamanho) {
  printf("função salvar tarefa");
  return OK;
}
Erro carregar(Tarefa t[], int *pos, int tamanho) {
  printf("função carregar tarefa");
  return OK;
}